#include "Image.h"

void cs225::Image::Darken() {
    unsigned int width = PNG::width();
    unsigned int height = PNG::height();
    /*
    iterate through every pixel -> any function?    
    then use the setter function to decrement the darkness
    HSLAPixel ^
    */
   for (int i = 0; i < width; i++) {
    for (int j = 0; j < height; j++) {
        
    }
   }
}

